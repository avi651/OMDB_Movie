import 'package:flutter/material.dart';

class OMDBMovieNotesScreen extends StatelessWidget {
  const OMDBMovieNotesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
